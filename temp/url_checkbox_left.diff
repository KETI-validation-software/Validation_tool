diff --git a/tests/test_url_selection_mode.py b/tests/test_url_selection_mode.py
--- a/tests/test_url_selection_mode.py
+++ b/tests/test_url_selection_mode.py
@@ -69,5 +69,21 @@ class UrlSelectionModeTests(unittest.TestCase):
 
         self.assertFalse(widget.show_checkbox)
         self.assertFalse(hasattr(widget, "checkbox_label"))
+
+    def test_checkbox_widget_can_place_checkbox_on_left(self):
+        widget = ClickableCheckboxRowWidget(
+            "192.168.1.70:2000",
+            0,
+            1,
+            "assets/image/test_config/row.png",
+            "assets/image/test_config/row_selected.png",
+            "assets/image/test_config/checkbox_unchecked.png",
+            "assets/image/test_config/checkbox_checked.png",
+            checkbox_alignment="left",
+        )
+
+        layout = widget.layout()
+
+        self.assertEqual(widget.checkbox_alignment, "left")
+        self.assertIs(layout.itemAt(0).widget(), widget.checkbox_label)
+        self.assertIs(layout.itemAt(1).widget(), widget.text_label)
 
 
 if __name__ == "__main__":
diff --git a/ui/widgets.py b/ui/widgets.py
--- a/ui/widgets.py
+++ b/ui/widgets.py
@@ -112,6 +112,7 @@ class ClickableCheckboxRowWidget(QWidget):
         checkbox_checked_path,
         parent=None,
         show_checkbox=True,
+        checkbox_alignment="right",
     ):
         super().__init__(parent)
         self.row = row
@@ -119,6 +120,7 @@ class ClickableCheckboxRowWidget(QWidget):
         self._text = text
         self._is_checked = False
         self.show_checkbox = show_checkbox
+        self.checkbox_alignment = checkbox_alignment
         self.setCursor(Qt.PointingHandCursor)
         self.setFixedHeight(39)
 
@@ -143,13 +145,17 @@ class ClickableCheckboxRowWidget(QWidget):
                 color: #000000;
             }
         """)
-        layout.addWidget(self.text_label, 1)
 
         if self.show_checkbox:
             self.checkbox_label = QLabel()
             self.checkbox_label.setStyleSheet('background: transparent;')
             self._update_checkbox_pixmap()
-            layout.addWidget(self.checkbox_label, 0, Qt.AlignRight | Qt.AlignVCenter)
+
+        if self.show_checkbox and self.checkbox_alignment == "left":
+            layout.addWidget(self.checkbox_label, 0, Qt.AlignLeft | Qt.AlignVCenter)
+
+        layout.addWidget(self.text_label, 1)
+
+        if self.show_checkbox and self.checkbox_alignment != "left":
+            layout.addWidget(self.checkbox_label, 0, Qt.AlignRight | Qt.AlignVCenter)
 
     def _update_checkbox_pixmap(self):
         if not self.show_checkbox or not hasattr(self, 'checkbox_label'):
diff --git a/ui/info_GUI.py b/ui/info_GUI.py
--- a/ui/info_GUI.py
+++ b/ui/info_GUI.py
@@ -1003,7 +1003,8 @@
                 url_widget = ClickableCheckboxRowWidget(
                     normalized_url, row, 1,
                     bg_image, bg_selected_image,
                     checkbox_unchecked, checkbox_checked,
-                    show_checkbox=_should_show_url_checkbox(getattr(self, 'target_system', ''))
+                    show_checkbox=_should_show_url_checkbox(getattr(self, 'target_system', '')),
+                    checkbox_alignment="left"
                 )
@@ -2056,7 +2057,8 @@
             url_widget = ClickableCheckboxRowWidget(
                 final_url, row, 1,
                 bg_image, bg_selected_image,
                 checkbox_unchecked, checkbox_checked,
-                show_checkbox=_should_show_url_checkbox(getattr(self, 'target_system', ''))
+                show_checkbox=_should_show_url_checkbox(getattr(self, 'target_system', '')),
+                checkbox_alignment="left"
             )