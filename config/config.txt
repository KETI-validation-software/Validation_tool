<?xml version="1.0" encoding="utf-8"?>
<config>
    <selected_auth type="Bearer Token">Bearer Token</selected_auth> <!-- type 속성 추가 -->
    <url>http://127.0.0.1:8008</url> <!-- 시험 URL -->
    <auth type="Bearer Token">  <!-- 사용자 인증 방식 정의 (지원 타입:Bearer Token) -->
        <bearer_token>abcd1234</bearer_token>
    </auth>
    <auth type="Digest Auth">  <!-- 사용자 인증 방식 정의 (지원 타입:Digest Auth) -->
        <username></username>
        <password></password>
    </auth>
</config>