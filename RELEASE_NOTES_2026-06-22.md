# Validation Tool — 베타 릴리즈 (2026-06-22)

> 이전 베타(2026-04-15) 이후 변경 사항

## 변경 사항

### 업데이트

#### 결과 페이지 / 시험 진행
- 시험 진행 중 인덱스 범위 초과로 검증이 중단되던 IndexError 가드 추가 (update_view)
- 검증 오류 PASS 케이스에도 `[검증 회차 N/M]` 헤더 표시 (FAIL과 통일)
- 플랫폼/시스템 시작 페이지 시험 정보 레이아웃·시나리오 패널 높이·필드 순서 정렬
- 시험 분야 테이블 격자선 중복·선택 시 구분선 소실 수정, 스크롤바 미표시 시 구분선 끝까지 표시
- 시험 분야명이 길어 잘릴 경우 왼쪽 정렬 + 말줄임 처리 및 hover 팝업으로 전체 표시

#### 타이머 / 모니터 표시
- 타이머 셀 위젯 in-place 갱신으로 잔상·깜빡임 완전 제거
- 요청/로그 전환 시 플랫폼 타이머가 리셋되던 문제 수정
- 웹훅 타이머가 창 시간(0→15)만큼 카운트업 후 0으로 떨어지던 문제 제거
- JSON 모니터 렌더링을 줄 단위 테이블 레이아웃으로 개선, 빈 payload `{}` 여러 줄 표시
- 모니터 응답시간 중복 텍스트 제거

#### 점수 계산
- 점수 요약 카드 폰트·아이콘·레이아웃 정렬 다듬기, 점수박스 구분선·표기 정리
- 점수 소수점 1자리로 통일
- 플랫폼 웹훅 타이머·점수 요약 UI·결과 컬럼 순서 버그 일괄 수정

#### 하트비트 / 웹훅
- 웹훅 이벤트 대기 창을 고정 10초 대신 구독요청 `duration`(초) 기준으로 동적 계산 (없으면 10초 폴백)
- 웹훅 윈도우 내 복수 이벤트 집계 구현
- 플랫폼 웹훅 모니터 로그를 창 종료 후 일괄이 아닌 도착 즉시 실시간 렌더
- 시스템 웹훅 모니터 결과 흐름을 플랫폼과 일치하도록 정렬
- 웹훅 검증 카운트·순서·응답시간 정정 및 송신 안정화
- 웹훅 프로토콜 응답 소요 시간을 일반 응답 수신 기준으로 변경
- 웹훅 API 상세 페이지 메시지 데이터 미표시 버그 수정
- 웹훅 모니터 이벤트 번호 suffix 표시 수정, 메시지 라벨에서 `#` 기호 제거
- 웹훅 송신 불가(UnboundLocalError) 회귀 수정
- 완료 하트비트가 전체 분야 완료 전에 전송되던 버그 수정

#### 기타
- ARP 스캔 시 잘못된 네트워크 인터페이스 선택 버그 수정
- 검증 시작 전 관리자 코드 갱신
- 스펙 로딩 및 최종 결과 전송 흐름 안정화
- 웹훅 비교 분석 기능 및 스펙 필드 추가

#### 코드 품질 개선
- systemVal `on_group_selected` 중복 정의 제거
- 콘솔 빌드 및 노트북 테스트 환경 설정 추가

## 디버그 레벨 안내

| 레벨 | 이름 | 출력 내용 |
|------|------|-----------|
| Level 1 | WARN | 에러 + 중요 정보 (API 요청/응답 요약) |
| Level 3 | DEBUG | 모든 디버그 정보 출력 (상세) |

## 실행 방법

1. exe 파일과 **config.txt** 를 같은 폴더에 다운로드
2. config.txt에서 관리자시스템 URL 설정
3. exe 더블클릭으로 실행

## config.txt 설정 예시

```ini
[Management]
url=http://ect2.iptime.org:20223

[Test]
test_ip=192.168.1.100

- url: 관리자시스템 주소
- test_ip: 시험 대상 IP
```

## 빌드 환경

- Python 3.9.13
- PyInstaller 5.13.2
- Windows 10

## 빌드 명령어

```ini
pyinstaller --onefile --windowed --splash=assets/image/splash/splash.png \
  --name ValidationTool_onefile_Level1 \
  --add-data "assets;assets" --add-data "config;config" \
  --add-data "core;core" --add-data "spec;spec" --add-data "ui;ui" main.py
```

> Level3 빌드 시 --windowed 제거, --console 추가, config/CONSTANTS.py의 DEBUG_LEVEL을 3으로 변경
