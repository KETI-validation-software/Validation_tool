# Todo Clarification Log

- Date: 2026-03-30
- Source: `memory/todo.md` 기준으로 모호한 요청을 구체화한 정리 문서

## 목적
- `memory/todo.md`의 항목 중 해석이 갈릴 수 있는 내용을 명확히 정리한다.
- 구현 우선순위와 확인 포인트를 분리해 작업/검증 흐름을 단순화한다.

## 항목별 정리

### 1. 시스템 2페이지에서 내 IP 노출 문제
- 질문:
  - `3번`의 "시스템에서 자기 ip가 나오고 있음 (2페이지에서)"의 정확한 의미가 무엇인지
  - 현재 URL의 host를 보여주는 것인지, 로컬 NIC IP를 보여주는 것인지 혼동됨
- 의도:
  - 시스템 2페이지(시나리오 선택 화면)에서 사용자 PC의 로컬 IP가 노출되지 않아야 함
  - 시스템 시험 대상 IP(또는 시험 URL host 기준 값)만 보이도록 맞춰야 함
- 확인 내용:
  - 현재 IP 표시는 `2페이지 URL 표시 값`과 연결해 해석
  - 즉, 시스템/플랫폼 모두 URL host 기준으로 표시되는지 점검 필요
- 답변:
  - 위 방향으로 이해함
- 정리:
  - 시스템 2페이지 URL 기준으로 표시되는 값이 사용자 로컬 IP로 보이지 않게 수정
  - 표시 기준은 시험 대상 IP로 통일

### 2. 3/4페이지 완료 상태 계산 기준
- 질문:
  - `6번` 타이머 변경과 별개로 상태 계산 로직도 변경된 것처럼 보이는데,
    API 결과가 있어도 상태가 `진행중`으로 남는 경우가 있어 기준 확인 필요
- 의도:
  - 완료 판정은 `모든 하위 항목 완료`일 때만 완료
  - 일부 시나리오/분야가 남아 있으면 완료가 아니라 진행중으로 유지
- 확인 내용:
  - 상태 계산은 타이머/timeout 노출과 분리해야 함
  - 즉 `시간 표시 문제`와 `완료 상태 계산`을 분리해서 점검
- 답변:
  - 상태 계산 로직을 우선 점검하고, 타이머 표시는 별도 확인
- 정리:
  - 3페이지와 4페이지 모두 `모든 시험 종료 시 완료`로 일관되게 처리
  - 부분 완료는 완료로 승격하지 않음
  - 타이머 이슈는 상태 로직과 독립적으로 처리

### 3. 3페이지 상세와 4페이지 상세 스키마 통일
- 질문:
  - `8번`에서 4페이지 상세 정보 스키마가 3페이지와 다른 것 같다는 이슈,
    두 화면의 키/필드 구조가 정말 동일해야 하는지
- 의도:
  - 사용자 관점에서 동일 개념은 같은 구조로 보여야 함
  - 3페이지와 4페이지의 상세 표현/키 체계를 맞춰 혼란을 줄임
- 확인 내용:
  - 단순 문구가 아니라 `키 이름`, `값 형식`, `빈 값 처리`, request/response/webhook 표시 방식까지 동일 기준 필요
- 답변:
  - 동일 기준으로 맞추는 방향 확정
  - 3페이지 기준을 4페이지에도 동일 적용
- 정리:
  - 4페이지 상세 출력 스키마를 3페이지와 동일 규격으로 정렬

### 4. 화면 강제 종료 시 error heartbeat 처리
- 질문:
  - `14번`에서 화면강제종료는 오류 상태인데 서버로 어떤 heartbeat를 보내야 하는지
  - 고려 대상:
    - 시험 중 앱 강제 종료
    - 네트워크 단절 후 복구 실패
    - 사용자 강제 종료
    - PC 절전/재부팅
  - 각 케이스에서 동일 처리인지 구분이 필요한지
- 의도:
  - 비정상 종료를 서버에서 명확히 인지할 수 있어야 함
  - 정상 heartbeat와 구분되는 오류 heartbeat 규약 필요
  - 종료 원인 분류가 가능하도록 최소 필드 정의 필요
- 확인 내용:
  - 두 가지로 정리
    1. 상태값 자체를 `강제 종료 + error heartbeat`로 명시할지,
    2. 기존 상태값은 유지하고 `error` 필드를 추가할지
- 답변:
  - 세부 프로토콜은 추가 합의 필요
- 정리:
  - 우선 비정상 종료 케이스를 상태/원인 코드로 식별 가능하게 전송
  - 정상 heartbeat와 별도로 `error heartbeat`를 구분해 처리
- 추가 확인 필요:
  - 상태값을 별도 `error`로 둘지
  - 기존 heartbeat 스키마 확장 범위
  - 서버 수신 측 heartbeat payload 계약

### 5. 긴 시험명 UI 잘림
- 질문:
  - `15번`의 긴 시험명 잘림 문제, 어디까지 줄여야 하는지
    - 2페이지 분야
    - 2페이지 시나리오
    - 3페이지 카드 타이틀
    - 4페이지 테이블 셀
  - 동일 정책 적용 범위 확인 필요
- 의도:
  - 긴 텍스트에서도 가독성을 유지하면서 pass/fail 인지 가능해야 함
- 정리:
  - `15번`은 UI 정책 항목으로 분리 관리

### 6. 점수 API 매핑
- 질문:
  - `17번`의 점수 항목에서 `전체 점수 / 분야별 점수 / 시나리오별 점수`를
    어떤 API 필드에 매핑하는지 명확히 정의가 필요한지
- 의도:
  - 점수 표시 API 연동 기준 확정
  - 필드 명칭 혼용으로 인한 매핑 오류 방지
  - 화면별 점수 표시는 동일 JSON 기준을 사용
  - `finalScore -> 전체 점수(총합), groupResults -> 분야별 결과 목록, groupScore -> 분야별 점수, scenarioResults -> 시나리오별 결과 목록, scenarioScore -> 시나리오 점수`로 매핑
- 정리:
  - 화면 표시와 데이터 저장 모두 같은 매핑 규칙으로 적용
  - 누락 필드는 기본값 처리 규칙을 별도 정의
  - 핵심 키:
    - `finalScore`
    - `groupResults`
    - `groupScore`
    - `scenarioResults`
    - `scenarioScore`

### 7. 점수 UI 용어 통일
- 질문:
  - 각 화면 UI에서 용어를 어떻게 통일할지
    - 3페이지
    - 4페이지
    - 3, 4페이지 간 문구 일치 여부
  - 현재 문구가 화면마다 달라 사용자 혼선 가능
- 의도:
  - 4페이지뿐 아니라 전 화면에서 용어 통일 필요
  - 내부 필드명과 사용자 노출 문구를 분리하되 일관성 유지
- 확인 내용:
  - 현재 4페이지는 `점수 정보` 대신 `평가 결과` 계열 문구가 섞여 있음
  - 최종 문구 후보는 다음으로 통일
    - `전체 점수 / 분야별 점수 / 시나리오별 점수`
    - `시험 상태 / 진행 상태 / 완료 상태`
    - 한 화면 내 같은 의미에 서로 다른 용어를 사용하지 않음
- 답변:
  - 용어 통일 방향 확정, 화면별 반영 필요
- 정리:
  - 전 화면 UI 문구 일관화
  - 점수는 `전체 점수 / 분야별 점수 / 시나리오별 점수`로 고정

### 8. 상세/원본보기 버튼 노출 정책
- 질문:
  - 4페이지 `상세 보기` 클릭 시 `원본 메시지 보기 1건` 같은 버튼이 보이는 조건
  - 오류 응답/정상 응답마다 노출 기준이 다른지 확인 필요
- 의도:
  - 조건에 따라 버튼 노출/비노출이 일관되게 동작해야 함
  - 빈 데이터에서 버튼만 남는 경우 방지
- 정리:
  - `상세 보기`는 데이터 존재 시에만 노출
  - `원본보기 버튼`은 원본 페이로드가 있을 때만 노출
  - `빈 상태`는 빈 상태 문구로 일관 처리

### 9. 4페이지 진행 상태와 색상 정책
- 질문:
  - 현재 4페이지 상태 색상/텍스트 매핑이 혼재되어 보임
  - `대기 중`과 `진행 중`의 구분 기준
    - 초기 진입 상태
    - 응답 수신 전/후 상태
    - 오류 발생 후 재시도 상태
    - 완료 후 상태 변경 허용 여부
- 의도:
  - 한 상태값은 한 가지 시각 규칙만 갖도록 정리
- 정리:
  - 4페이지 상태 표기 및 색상 정책을 단일 규칙으로 통일

### 10. 작업 우선순위
- 질문:
  - 이번 배포 전 꼭 확인해야 할 항목 순서를 정리하면,
    우선순위는 아래와 같음
    1. 완료 상태 계산 기준 `모든 하위 완료`
    2. 시스템 2페이지 URL 기준 IP 표시 정합
    3. 3페이지/4페이지 상세 스키마 통일
    4. 4페이지 점수 UI 문구 통일
    5. 화면 강제 종료 시 `error heartbeat` 처리
    - 위 순서대로 점검하면 재작업을 줄일 수 있음
- 의도:
  - 위험도 높은 항목부터 순차 처리
- 정리:
  - 위 우선순위에 따라 진행

## 최종 우선순위

### 즉시 처리 필요
1. 3/4페이지 완료 상태 계산 기준을 `모든 하위 완료`로 통일
2. 시스템 2페이지 URL 기준 값이 로컬 IP로 보이지 않게 수정
3. 4페이지 상세를 3페이지와 동일 스키마로 통일
4. 4페이지 점수 UI를 `전체/분야별/시나리오별 점수`로 통일

### 추가 합의 필요
5. 화면 강제 종료 시 `error heartbeat` 상태/필드 규약 확정

### 단순 개선 항목
- 긴 시험명 잘림 대응(`15번`)

## 참고 메모
- `todo.md`의 원문 항목을 기준으로 `질문 -> 의도 -> 정리` 형태로 재구성했다.
- `14번`은 서버-클라이언트 계약이 필요한 항목으로, 별도 합의 후 확정 구현이 필요하다.
